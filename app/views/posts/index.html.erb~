<h1>ブログ詳細画面</h1>

<br />

<% @posts.each do |post| %>
<table>
  <tr>
    <td colspan="3">作成者:<%= post.name %></td>
  </tr>
  <tr>
    <td colspan="3">題名:<%= post.title %></td>
  </tr>
  <tr>
    <td colspan="3">作成日時:<%= post.created_at.strftime("%Y年 %m月 %d日 %H:%M") %></td>
  </tr>
  <tr>
    <td colspan="3">更新日時:<%= post.updated_at.strftime("%Y年 %m月 %d日 %H:%M") %></td>
  </tr>
  <tr>
    <td><%= link_to '閲覧', post %></td>
    <td><%= link_to '編集', edit_post_path(post) %></td>
    <td><%= link_to '削除', post, method: :delete, data: { confirm: '削除しますがよろしいですか?' } %></td>
  </tr>
</table>
<br />
<% end %>


<br />

<%= link_to '記事の新規作成', new_post_path %>
