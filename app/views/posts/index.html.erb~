<h1>ブログ詳細画面</h1>

<table>
  <tr>
    <th>名前</th>
    <th>題名</th>
    <th>更新日時</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @posts.each do |post| %>
  <tr>
    <td><%= post.name %></td>
    <td><%= post.title %></td>
    <td><%= modfi %></td>
    <td><%= link_to '閲覧', post %></td>
    <td><%= link_to '編集', edit_post_path(post) %></td>
    <td><%= link_to '削除', post, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to '記事の新規作成', new_post_path %>
